#!/usr/bin/env perl

use strict;
use Juman;

my $juman = new Juman(-Command => '/home/harashima/usr/bin/juman',
		      -Rcfile => '/home/harashima/usr/etc/jumanrc');

while (<>) {
    my ($midasi, @words) = @_;



}
